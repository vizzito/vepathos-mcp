# Vepathos MCP — tools reference

Version 0.8.0.

GENERATED by `vepathos-mcp tools --write-docs` from the tool registry. Do not edit.

Rendered with every optional tool enabled and `MCP_CONFIRM_BEFORE_OPTIMIZE=false` (the production default). Behaviour, errors and examples: [tools.md](tools.md).

## Tools by task

- **Account**: `get_account`
- **Plan and run**: `optimize_delivery_routes`, `optimize_plan`, `list_plans`, `get_optimization_result`
- **Imports**: `import_delivery_file`, `import_delivery_text`, `get_import_result`, `update_import_mapping`, `list_datasets`
- **Addresses**: `geocode_addresses`, `get_geocode_result`
- **Saved vehicles and depots**: `list_fleet`, `manage_vehicle`, `manage_depot`
- **Automations**: `list_automations`, `create_automation`
- **Share**: `create_optimization_map`

## Workflows

**Stops given in the conversation.** The user pastes or dictates addresses or coordinates.

`get_account → list_fleet → geocode_addresses (street addresses only) → get_geocode_result → optimize_delivery_routes → get_optimization_result`

**A file or pasted rows.** The orders are in a spreadsheet, a link or pasted text.

`import_delivery_file | import_delivery_text → get_import_result → update_import_mapping (only when a column was read wrong) → optimize_plan → get_optimization_result`

**Run a saved plan again.** The stops are already saved in Vepathos.

`list_plans | list_datasets → optimize_plan → get_optimization_result`

**Share a result.** The user wants a link anyone can open.

`get_optimization_result → create_optimization_map`

**Prepare an automation.** The user wants a plan to route on a schedule.

`list_plans → list_automations → create_automation (always created switched off)`

**Save a vehicle or a depot.** The user asks to add, save or edit one in their account; never for one plan's settings.

`list_fleet → manage_vehicle | manage_depot (a depot given as an address goes through geocode_addresses first)`

## `optimize_delivery_routes`

**Optimize delivery routes**

Plan optimized delivery routes for stops given in this conversation (vehicle routing problem, VRP); stops already saved in Vepathos run through optimize_plan instead. Assigns each stop to a vehicle and sequences every route from one depot, minimizing total distance while respecting the constraints you provide: maximum stops per vehicle, weight capacity (kg), volume capacity (m3) and delivery time windows. Built for large problems, from dozens to thousands of stops. Every stop needs latitude and longitude; addresses go through geocode_addresses first. Runs asynchronously: returns an optimization_id, plus the result when the optimization finishes within a few seconds; use get_optimization_result to retrieve status and routes. Each call saves the run as a new plan in the user's Vepathos account (plan_id, account_url; name it with plan_name). Tell the user when plan_replaced or plan_temporary is set. Every call runs and charges one plan stop per stop sent, so confirm first: tell the user how many stops it charges and how many remain, the vehicles and the constraints it enforces, which columns of their data are not being sent, and that arrival times are absent unless schedule.route_start_time is set. Then call once. Identical arguments are deduplicated and charged once, but any change, including a different max_stops or vehicle count on the same stops, is a new optimization in a new plan and charges again. A request that exceeds the account plan, or needs a constraint it lacks, is rejected with an explanation and never partially applied. To vary a run, call optimize_plan with its plan_id: within 24 h the same stops or fewer are another try, no stops charged.

- Read only: no · Destructive: yes · Idempotent: yes · Open world: no
- Published: always
- Workflows: Stops given in the conversation

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `depot` | object | yes | Where every route starts. |
| `depot.latitude` | number | yes | Depot latitude in decimal degrees (WGS84). |
| `depot.longitude` | number | yes | Depot longitude in decimal degrees (WGS84). |
| `vehicles` | array of object | yes | Available fleet (1-50 vehicle types). |
| `vehicles[].vehicle_id` | string | yes | Your id for this vehicle or vehicle type (letters, digits, '_', '-', '.'; max 32). Echoed on every route it drives. |
| `vehicles[].count` | integer | no | Number of identical vehicles of this type available for this plan. A plan setting: it never adds vehicles to the account. |
| `vehicles[].min_stops` | integer | no | Minimum stops one vehicle should serve. Omitted: 50% of max_stops. It must stay at least 10% under max_stops; a higher value is lowered to floor(max_stops x 0.9), with a warning. |
| `vehicles[].max_stops` | integer | no | Maximum number of stops one vehicle may serve on its route. |
| `vehicles[].max_weight_kg` | number | no | Payload capacity per vehicle in kilograms. Setting it on any vehicle enforces weight capacity (every vehicle and stop then needs weight) unless use_weight=false. |
| `vehicles[].max_volume_m3` | number | no | Cargo volume capacity per vehicle in cubic meters. Setting it on any vehicle enforces volume capacity (every vehicle and stop then needs volume) unless use_volume=false. |
| `stops` | array of object | yes | Stops to assign and sequence (at least 1). |
| `stops[].stop_id` | string | yes | Your id for this stop or order (max 64 characters). Unique. Echoed in results. |
| `stops[].latitude` | number | yes | Stop latitude in decimal degrees (WGS84). |
| `stops[].longitude` | number | yes | Stop longitude in decimal degrees (WGS84). |
| `stops[].weight_kg` | number | no | Total weight delivered at this stop, kg. |
| `stops[].volume_m3` | number | no | Total volume delivered at this stop, cubic meters. |
| `stops[].time_window` | object | no | Delivery time window for this stop. |
| `stops[].time_window.start` | string | yes | Earliest arrival, local time HH:MM (24h). |
| `stops[].time_window.end` | string | yes | Latest arrival, local time HH:MM (24h). |
| `schedule` | object | no | Optional date, departure time and time zone. |
| `schedule.date` | string | no | Delivery date YYYY-MM-DD in time_zone. Defaults to today. |
| `schedule.route_start_time` | string | no | Time routes leave the depot, local HH:MM. Required when any stop has a time_window. |
| `schedule.time_zone` | string | no | IANA time zone for dates and times, e.g. America/New_York. |
| `schedule.service_time_minutes` | number | no | Minutes spent at each stop (unloading, hand-off). |
| `schedule.max_route_minutes` | number | no | Maximum journey length per route in minutes (travel + service). Activates the engine time cap when set. |
| `use_weight` | boolean | no | Optimize by weight capacity. Omit to follow the data; false keeps weights for reference. |
| `use_volume` | boolean | no | Optimize by volume capacity. Omit to follow the data; false keeps volumes for reference. |
| `max_load_ratio` | number | no | Highest share of each vehicle's weight/volume capacity to fill. Default 0.95 (5% margin); 1 only when the user asks to fill vehicles completely. |
| `use_time_windows` | boolean | no | Respect stop time windows. Omit to follow the data; false keeps windows for reference. |
| `idempotency_key` | string | no | Optional key to deduplicate retries. By default identical arguments are deduplicated. |
| `plan_name` | string | no | Name of the plan this run is saved as in the user's account. Default: Optimization YYYY-MM-DD. |
| `depot_name` | string | no | Depot name shown in the plan. |

### Output

| Field | Type | Description |
|---|---|---|
| `optimization_id` | string | Handle for get_optimization_result. |
| `plan_id` | string | The plan this lives in, in the user's Vepathos account (list_plans). |
| `plan_name` | string | Name of that plan. |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |
| `plan_replaced` | any | Set when the library was full: this plan was removed to make room. Tell the user its name. |
| `plan_temporary` | boolean | True when every library slot is kept or favorite: the plan lives outside the library and is deleted later. Tell the user. |
| `status` | queued \| running \| completed \| failed | queued / running / completed / failed. Absent on a preflight-only reply. |
| `idempotent_replay` | boolean | True when identical arguments returned an optimization that already existed. |
| `submitted_stops` | integer | Stops accepted into this job. |
| `vehicles_available` | integer | Fleet units declared (sum of vehicles[].count). |
| `schedule_date` | string | Date used for the run, YYYY-MM-DD. |
| `expires_at` | string | While running: when an unfinished run is dropped (UTC). Completed runs stay. |
| `stops_remaining_this_period` | integer | Stops left in the plan's current billing period (null means unlimited). |
| `quota_charged` | boolean | False when this run is another try of the plan or the trial: no stops charged. |
| `free_retry` | boolean | True when this run is another try (no monthly stops charged). Say 'another try' / 'otro intento'; never call it free. |
| `free_retries_remaining` | integer | Other tries of this plan still available after this run: within 24 h, same stops or fewer, by plan_id. Say 'other tries' / 'otros intentos'; do not call the run free. |
| `full_trial_applied` | any | Present when this job used the one-time MCP full trial. |
| `progress` | any | Present while queued or running. |
| `poll_after_seconds` | integer | Call get_optimization_result again after about this many seconds. |
| `result` | any | Present when the optimization finished within the call. |
| `depot_resolved` | any | Present when the depot was given as an address: tell the user the matched address the routes start from. |
| `preflight` | any | Present instead of an optimization when confirmed was false: nothing ran, nothing was charged. |
| `error` | object | Structured error when the tool call failed. |

## `get_optimization_result`

**Get optimization result**

Get the status and outcome of a route optimization by its optimization_id. While it runs, returns status and progress: tell the user the percent and the stage in one line, and wait poll_after_seconds before asking again. When complete, detail=summary returns totals and per-route metrics; detail=stops returns ordered stop_ids with arrival times (driver clock, service at earlier stops included). request shows the depot, vehicles and schedule it ran with. Completed results are kept with their plan: plan_id, and account_url, which opens it in the user's Vepathos account (sign-in required). Read-only; does not consume plan stops.

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Stops given in the conversation; A file or pasted rows; Run a saved plan again; Share a result

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `optimization_id` | string | yes | The optimization_id returned by optimize_delivery_routes. |
| `detail` | summary \| stops \| unassigned | no | summary: totals and per-route metrics. stops: ordered stops with arrival times. unassigned: ids of stops that could not be routed. |
| `route_id` | string | no | Only for detail=stops: return the stop sequence of this route only. |
| `offset` | integer | no | Pagination offset (routes or stops). |
| `limit` | integer | no | Page size. Defaults: 25 routes for summary, 200 items for stops and unassigned. |

### Output

| Field | Type | Description |
|---|---|---|
| `optimization_id` | string | Same handle returned by optimize. |
| `status` | queued \| running \| completed \| failed | queued / running / completed / failed. |
| `plan_id` | string | The plan this lives in, in the user's Vepathos account (list_plans). |
| `history_id` | string | This run in the plan's history. |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |
| `detail` | summary \| stops \| unassigned | Which slice of the result this payload carries. |
| `progress` | any | Present while queued or running. |
| `poll_after_seconds` | integer | When status is queued or running, call again after about this many seconds. |
| `expires_at` | string | When results stop being available (UTC). Absent: kept with the plan. |
| `request` | object | What this optimization ran with: depot, vehicles, schedule, objective, dataset. Reuse it to repeat or vary the run; confirm the depot and departure with the user first. |
| `summary` | any | Present when detail=summary and completed. |
| `routes` | array of any | Per-route metrics page when detail=summary. |
| `stops` | array of any | Ordered visits when detail=stops (no coordinates echoed). |
| `unassigned_stop_ids` | array of string | Ids that could not be routed when detail=unassigned. |
| `page` | any | Pagination for routes or stops. |
| `error` | object | Structured error when the tool call failed. |

## `list_plans`

**List plans**

List the plans in the connected Vepathos account (the dashboard's plans; every optimization is saved in one). Without plan_id: favorites first, then newest, plus the library size; a full library replaces its oldest plan not kept or favorite. With plan_id: stop counts, totals, depot, whether the next run charges stops, other tries left and window end, and last_agent_run. When summarizing for the user: if next_optimize_charged is false say 'another try' / 'otro intento' with tries remaining and the deadline; if true say it charges N stops — never call a run free. account_url opens a plan (sign-in required). Read-only; never returns stops. If they ask for their Vepathos tasks, jobs or functions, call this (saved plans), not the host's scheduled-task list; summarize plan names and counts, do not dump this server's tool names. To rerun or vary one, confirm last_agent_run with the user and call optimize_plan with plan_id.

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Run a saved plan again; Prepare an automation

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `plan_id` | string | no | Return this plan only, with last_agent_run (what it last ran with). |
| `query` | string | no | Only plans whose name contains this text. |
| `limit` | integer | no | Plans to return, favorites first, then newest. |

### Output

| Field | Type | Description |
|---|---|---|
| `plans` | array of any | Without plan_id: the account's plans. |
| `library` | any |  |
| `plan` | any | With plan_id: that plan. |
| `error` | object |  |

## `optimize_plan`

**Optimize a plan**

Optimize stored stops: a plan (plan_id, from list_plans or get_import_result) or an import (dataset_id), exactly one, saving the run in that plan. Pass depot (coordinates, or an address: tell the user the depot_resolved match), vehicles and per-run options (exclude_stop_ids for this run only, use_weight/volume/time_windows, service_time_minutes, max_route_minutes, min_stops/max_stops). A charged run spends monthly stops. Within 24 h of that run, the same plan with the same stops or fewer (id and coordinates) is another try — no stops charged; plan limits still apply (Spanish 'otro intento'; never call it free). next_optimize_charged and charged_because say which applies. Confirm with the user (charge or another try), then call once. Tell the user when plan_replaced or plan_temporary is set. Poll get_optimization_result.

- Read only: no · Destructive: yes · Idempotent: yes · Open world: no
- Published: always
- Workflows: A file or pasted rows; Run a saved plan again

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `plan_id` | string | no | The plan to optimize (list_plans, get_import_result). |
| `dataset_id` | string | no | An import's stops, instead of plan_id. |
| `depot` | object | yes |  |
| `depot.latitude` | number | no |  |
| `depot.longitude` | number | no |  |
| `depot.address` | string | no | Depot as an address; the server geocodes it and shows what it found (depot_resolved). An uncertain match is refused until the user confirms its coordinates. |
| `depot.city` | string | no | City of the depot address, when known. |
| `depot.country` | string | no | Country name or ISO code of the depot address, when known. |
| `vehicles` | array of object | yes |  |
| `vehicles[].vehicle_id` | string | yes |  |
| `vehicles[].count` | integer | no |  |
| `vehicles[].min_stops` | integer | no | Minimum stops per vehicle. Omitted: 50% of max_stops. It must stay at least 10% under max_stops; a higher value is lowered to floor(max_stops x 0.9), with a warning. |
| `vehicles[].max_stops` | integer | no |  |
| `vehicles[].max_weight_kg` | number | no |  |
| `vehicles[].max_volume_m3` | number | no |  |
| `exclude_stop_ids` | array of string | no | Stops to leave out of this run only; the plan keeps them. |
| `use_weight` | boolean | no | Optimize by weight. Omit to follow the vehicles' max_weight_kg; false keeps weights for reference. |
| `use_volume` | boolean | no | Optimize by volume. Omit to follow the vehicles' max_volume_m3; false keeps volumes for reference. |
| `max_load_ratio` | number | no | Highest share of each vehicle's weight/volume capacity to fill. Default 0.95 (5% margin); 1 only when the user asks to fill vehicles completely. |
| `use_time_windows` | boolean | no | Respect the stops' time windows. Omit to follow the data; false keeps them for reference. |
| `route_start_time` | string | no |  |
| `time_zone` | string | no |  |
| `service_time_minutes` | number | no |  |
| `max_route_minutes` | number | no | Maximum journey length per route in minutes. Activates the engine time cap. |
| `date` | string | no |  |
| `idempotency_key` | string | no |  |
| `depot_name` | string | no | Depot name shown in the plan. |

### Output

| Field | Type | Description |
|---|---|---|
| `optimization_id` | string | Handle for get_optimization_result. |
| `plan_id` | string | The plan this lives in, in the user's Vepathos account (list_plans). |
| `plan_name` | string | Name of that plan. |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |
| `plan_replaced` | any | Set when the library was full: this plan was removed to make room. Tell the user its name. |
| `plan_temporary` | boolean | True when every library slot is kept or favorite: the plan lives outside the library and is deleted later. Tell the user. |
| `status` | queued \| running \| completed \| failed | queued / running / completed / failed. Absent on a preflight-only reply. |
| `idempotent_replay` | boolean | True when identical arguments returned an optimization that already existed. |
| `submitted_stops` | integer | Stops accepted into this job. |
| `vehicles_available` | integer | Fleet units declared (sum of vehicles[].count). |
| `schedule_date` | string | Date used for the run, YYYY-MM-DD. |
| `expires_at` | string | While running: when an unfinished run is dropped (UTC). Completed runs stay. |
| `stops_remaining_this_period` | integer | Stops left in the plan's current billing period (null means unlimited). |
| `quota_charged` | boolean | False when this run is another try of the plan or the trial: no stops charged. |
| `free_retry` | boolean | True when this run is another try (no monthly stops charged). Say 'another try' / 'otro intento'; never call it free. |
| `free_retries_remaining` | integer | Other tries of this plan still available after this run: within 24 h, same stops or fewer, by plan_id. Say 'other tries' / 'otros intentos'; do not call the run free. |
| `full_trial_applied` | any | Present when this job used the one-time MCP full trial. |
| `progress` | any | Present while queued or running. |
| `poll_after_seconds` | integer | Call get_optimization_result again after about this many seconds. |
| `result` | any | Present when the optimization finished within the call. |
| `depot_resolved` | any | Present when the depot was given as an address: tell the user the matched address the routes start from. |
| `preflight` | any | Present instead of an optimization when confirmed was false: nothing ran, nothing was charged. |
| `error` | object | Structured error when the tool call failed. |

## `import_delivery_file`

**Import delivery file**

Import a delivery file (Excel, CSV, JSON, text). Pass file when your host hands attachments to tools (ChatGPT: _meta openai/fileParams); otherwise pass url: a public https link, or a Google Drive, Sheets or Docs share link as copied. With neither, send the file's text to import_delivery_text. A path on the user's disk is not a url: this server cannot read it. When your host can read local files, send a CSV or JSON file's text to import_delivery_text; for a spreadsheet (.xlsx) ask for a share link or a CSV export, or let the user upload it in their Vepathos account, where it becomes a saved plan (list_plans, then optimize_plan). Do not shell out (curl/gdown/pip), and never parse or convert the file with a script. Returns import_id; the stops load into a new plan named after the file, or replace the stops of plan_id. Never paste thousands of stops into optimize_delivery_routes. Next: get_import_result until plan_id, then optimize_plan.

- Read only: no · Destructive: no · Idempotent: no · Open world: no
- Published: `MCP_IMPORT_TOOLS_ENABLED`
- Workflows: A file or pasted rows

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `file` | object | no | The attachment, filled in by hosts that hand files to tools (ChatGPT file params); never build it by hand. Other hosts pass url. |
| `file.download_url` | string | no |  |
| `file.file_id` | string | no |  |
| `file.mime_type` | string | no |  |
| `file.file_name` | string | no |  |
| `url` | string | no | Public https URL of the file, or a Google Drive, Sheets or Docs share link. For hosts without attachment parameters (Claude, Gemini, Cursor, a console). |
| `filename` | string | no |  |
| `timezone` | string | no |  |
| `depot_country` | string | no |  |
| `plan_id` | string | no | Load the stops into this existing plan, replacing its stops and keeping its depot, fleet and settings. Omit to create a new plan named after the file. |

### Output

| Field | Type | Description |
|---|---|---|
| `import_id` | string |  |
| `dataset_id` | string |  |
| `plan_id` | string | The plan the stops load into; null until they load (get_import_result). |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |
| `status` | string |  |
| `poll_after_seconds` | integer |  |
| `expires_at` | string |  |
| `error` | object |  |

## `import_delivery_text`

**Import pasted deliveries**

Import deliveries as text, through the same pipeline as a file, into a new plan or plan_id: rows the user pasted, or the text of a CSV or JSON file when the host gives no attachment parameter or link (a few hundred rows; larger files go by url). Returns import_id; next get_import_result, then optimize_plan.

- Read only: no · Destructive: no · Idempotent: no · Open world: no
- Published: `MCP_IMPORT_TOOLS_ENABLED`
- Workflows: A file or pasted rows

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes | Pasted delivery list. |
| `filename` | string | no |  |
| `timezone` | string | no |  |
| `plan_id` | string | no | Load the stops into this existing plan, replacing its stops and keeping its depot, fleet and settings. Omit to create a new plan named after the file. |

### Output

| Field | Type | Description |
|---|---|---|
| `import_id` | string |  |
| `dataset_id` | string |  |
| `plan_id` | string | The plan the stops load into; null until they load (get_import_result). |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |
| `status` | string |  |
| `poll_after_seconds` | integer |  |
| `expires_at` | string |  |
| `error` | object |  |

## `get_import_result`

**Get import result**

Status and summary of an import_delivery_file / import_delivery_text job. When complete: plan_id (the plan the stops loaded into), account_url, summary (counts, mapping, sample, needs_confirmation) and whether the next run is charged. Tell the user when plan_replaced or plan_temporary is set. status=needs_mapping: the import waits on the columns it is unsure of, each named in summary.rows_to_review with its suggested field. Answer EVERY one through update_import_mapping (the suggested field to confirm it, another to correct it, null to ignore it); changing other columns does not clear it. Check each suggestion against the sample values first: never confirm one that does not fit (dates suggested as phone) just to move on; ignore that column with null, or ask the user when it may matter to the routes. Never returns all rows. Then call optimize_plan with plan_id.

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: `MCP_IMPORT_TOOLS_ENABLED`
- Workflows: A file or pasted rows

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `import_id` | string | yes |  |

### Output

| Field | Type | Description |
|---|---|---|
| `plan_id` | string | The plan this lives in, in the user's Vepathos account (list_plans). |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |
| `plan_replaced` | any | Set when the library was full: this plan was removed to make room. Tell the user its name. |
| `plan_temporary` | boolean | True when every library slot is kept or favorite: the plan lives outside the library and is deleted later. Tell the user. |
| `next_optimize_charged` | boolean | True: the next run of this plan charges its stops to the monthly allowance. False: it is another try (same stops or fewer, within the 24 h window) — no stops charged. When speaking to the user: English 'another try', Spanish 'otro intento'; never call a run free. |
| `free_retries_allowed` | integer | How many other tries a charged run opens on this account plan (field name is historical; say 'other tries' / 'otros intentos', not that the run is free). |
| `free_retries_remaining` | integer | Other tries left in the open 24 h window. 0 when no window is open. Tell the user the count and the deadline (free_retry_window_ends_at); do not call the run free. |
| `free_retry_window_ends_at` | string | When the other-try window closes (UTC). Null when no window is open. Always mention this deadline when next_optimize_charged is false. |
| `charged_because` | string | Why the next run is charged: no_billed_run, stops_changed (a stop was added or moved), allowance_used, window_closed or no_allowance. |
| `import_id` | string |  |
| `dataset_id` | string |  |
| `status` | string |  |
| `summary` | object |  |
| `expires_at` | string | When dataset_id expires. The plan stays. |
| `poll_after_seconds` | integer |  |
| `progress` | object |  |
| `error` | object |  |

## `update_import_mapping`

**Update import mapping**

Correct an import's column mapping without re-uploading: {source_column: vepathos_field}, or {field, unit, format} for a column in another unit (lb, in, l) or date/number format. When the user corrects a unit ('those were cm3'), fix it here, never by converting the rows yourself. Returns the updated summary.

- Read only: no · Destructive: no · Idempotent: yes · Open world: no
- Published: `MCP_IMPORT_TOOLS_ENABLED`
- Workflows: A file or pasted rows

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `import_id` | string | yes |  |
| `mapping` | object | yes | Source column → vepathos field, null to ignore the column, or {field, unit, format} when its values are in another unit or format. |

### Output

| Field | Type | Description |
|---|---|---|
| `plan_id` | string | The plan this lives in, in the user's Vepathos account (list_plans). |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |
| `plan_replaced` | any | Set when the library was full: this plan was removed to make room. Tell the user its name. |
| `plan_temporary` | boolean | True when every library slot is kept or favorite: the plan lives outside the library and is deleted later. Tell the user. |
| `next_optimize_charged` | boolean | True: the next run of this plan charges its stops to the monthly allowance. False: it is another try (same stops or fewer, within the 24 h window) — no stops charged. When speaking to the user: English 'another try', Spanish 'otro intento'; never call a run free. |
| `free_retries_allowed` | integer | How many other tries a charged run opens on this account plan (field name is historical; say 'other tries' / 'otros intentos', not that the run is free). |
| `free_retries_remaining` | integer | Other tries left in the open 24 h window. 0 when no window is open. Tell the user the count and the deadline (free_retry_window_ends_at); do not call the run free. |
| `free_retry_window_ends_at` | string | When the other-try window closes (UTC). Null when no window is open. Always mention this deadline when next_optimize_charged is false. |
| `charged_because` | string | Why the next run is charged: no_billed_run, stops_changed (a stop was added or moved), allowance_used, window_closed or no_allowance. |
| `import_id` | string |  |
| `dataset_id` | string |  |
| `status` | string |  |
| `summary` | object |  |
| `expires_at` | string | When dataset_id expires. The plan stays. |
| `poll_after_seconds` | integer |  |
| `progress` | object |  |
| `error` | object |  |

## `list_datasets`

**List datasets**

List imports from this or earlier chats: the plan each loaded into (plan_id), whether its next run is charged, and last_run: the depot, vehicles and schedule of its latest optimization, to repeat or vary it once the user confirms them. Read-only.

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: `MCP_IMPORT_TOOLS_ENABLED`
- Workflows: Run a saved plan again

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `limit` | integer | no |  |

### Output

| Field | Type | Description |
|---|---|---|
| `datasets` | array of any |  |
| `error` | object |  |

## `geocode_addresses`

**Geocode addresses**

Turn street addresses into latitude/longitude using Vepathos Smart Import. Requires a depot lat/lng or a city. Each stop includes matched_address (what the gazetteer matched) — use it to catch bad pins. Charges Smart Import quota, not route stops. When needs_confirmation is true, tell the user which ids are missing or uncertain before optimizing. Do not invent coordinates. Returns pins, or a geocode_id for get_geocode_result; the coordinates then go to optimize_delivery_routes.

- Read only: no · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Stops given in the conversation

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `addresses` | array of object | yes | Stops to geocode (1-500). Uses the account Smart Import quota, not route stops. |
| `addresses[].stop_id` | string | yes | Your id for this stop (max 64 characters). Unique. Echoed in results. |
| `addresses[].address` | string | yes | Street address to geocode. Not a coordinate. |
| `addresses[].city` | string | no | City, when known. |
| `addresses[].region` | string | no | State, province or region. |
| `addresses[].postcode` | string | no |  |
| `addresses[].country` | string | no | Country name or ISO code, when known. |
| `depot` | object | no | Depot coordinates. Helps pick the map region. Required if city is omitted. |
| `depot.latitude` | number | yes | Depot latitude in decimal degrees (WGS84). |
| `depot.longitude` | number | yes | Depot longitude in decimal degrees (WGS84). |
| `city` | string | no | City used to pick the map region when depot is omitted. |
| `country` | string | no |  |
| `timezone` | string | no | IANA time zone, e.g. America/Argentina/Buenos_Aires. |

### Output

| Field | Type | Description |
|---|---|---|
| `geocode_id` | string | Handle for get_geocode_result. |
| `status` | queued \| running \| completed \| failed | queued / running / completed / failed. |
| `submitted_stops` | integer | Addresses sent to Smart Import. |
| `resolved_stops` | integer | Stops that received a latitude and longitude. |
| `unresolved_stop_ids` | array of string | Stops with no pin. Do not invent coordinates. Ask before optimizing. |
| `review_stop_ids` | array of string | Pins to confirm (band=review or confidence below 0.8). Ask before routing them. |
| `needs_confirmation` | boolean | True when any stop is unresolved or in review. Wait for the user before optimize. |
| `poll_after_seconds` | integer | Call get_geocode_result again after about this many seconds. |
| `progress` | any | Present while the geocode job is running. |
| `stops` | array of any | Per-stop pins when status is completed (includes matched_address). |
| `error` | object | Structured error when the tool call failed. |

## `get_geocode_result`

**Get geocode result**

Get the status and coordinates of a geocode_addresses job. Read-only. When complete, check needs_confirmation, unresolved_stop_ids and review_stop_ids before optimizing. Does not consume route-stop quota.

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Stops given in the conversation

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `geocode_id` | string | yes | The geocode_id returned by geocode_addresses. |

### Output

| Field | Type | Description |
|---|---|---|
| `geocode_id` | string | Handle for get_geocode_result. |
| `status` | queued \| running \| completed \| failed | queued / running / completed / failed. |
| `submitted_stops` | integer | Addresses sent to Smart Import. |
| `resolved_stops` | integer | Stops that received a latitude and longitude. |
| `unresolved_stop_ids` | array of string | Stops with no pin. Do not invent coordinates. Ask before optimizing. |
| `review_stop_ids` | array of string | Pins to confirm (band=review or confidence below 0.8). Ask before routing them. |
| `needs_confirmation` | boolean | True when any stop is unresolved or in review. Wait for the user before optimize. |
| `poll_after_seconds` | integer | Call get_geocode_result again after about this many seconds. |
| `progress` | any | Present while the geocode job is running. |
| `stops` | array of any | Per-stop pins when status is completed (includes matched_address). |
| `error` | object | Structured error when the tool call failed. |

## `get_account`

**Get connected account**

Show which Vepathos account this connection uses and what its plan allows: account label (company name, or a masked email), plan name, maximum stops per optimization, fleet and route limits, the constraints the plan includes, and the stops used and remaining in the current billing period. Takes no arguments: the account comes from the connection itself. Read-only and it does not consume plan stops. Call this immediately when the user asks which account, plan or quota is connected; never say you cannot see the account or that no profile tool exists. Use it to check limits before a large optimization, and to diagnose a rejection for the plan, the quota or authorization. When it is not the account the user expected, the fix is to reconnect as the right user, not to change the plan: revoke the old grant in Connected apps in the Vepathos dashboard, then connect again (connecting creates a free account when they have none).

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Stops given in the conversation

### Input

Takes no arguments.

### Output

| Field | Type | Description |
|---|---|---|
| `account_label` | string | Human-readable owner of the connected account: company name, or a masked email. |
| `account_id` | string | Vepathos account id. Use it to tell accounts apart. |
| `plan` | any |  |
| `usage` | any |  |
| `full_trial_available` | boolean | Whether the one-time full-feature optimization is still unused. |
| `error` | object |  |

## `list_fleet`

**List fleet**

List what the connected Vepathos account has saved: vehicles and fleets (capacity in kilograms and cubic meters, units per vehicle, and the ids to pass as vehicles[] to optimize_delivery_routes or optimize_plan) and depots (name and coordinates to pass as depot). Takes no arguments; read-only and it does not consume plan stops. empty=true means the account has no vehicles saved, so ask the user to describe them or let them add the fleet in the Vepathos dashboard. A vehicle with no capacity means the account never set one, not that it carries nothing.

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Stops given in the conversation; Save a vehicle or a depot

### Input

Takes no arguments.

### Output

| Field | Type | Description |
|---|---|---|
| `fleets` | array of any |  |
| `vehicles` | array of any | Vehicles in the account, including any that belong to no fleet. |
| `depots` | array of any | Depots saved in the account: where routes can start. |
| `empty` | boolean | True when the account has no fleet loaded: ask the user to describe the vehicles. |
| `error` | object |  |

## `manage_vehicle`

**Manage saved vehicles**

Add or change a vehicle saved in the user's Vepathos account: master data that stays after this conversation and shows in their dashboard. Call it only when the user asks to add, save or edit a vehicle ('add a 1,500 kg Sprinter', 'van 4 now carries 12 m3'). Not for one plan: how many vehicles a run uses, stops per vehicle, a capacity for today only or a vehicle that is out tomorrow are plan settings: pass them in vehicles[] to optimize_delivery_routes or optimize_plan and save nothing. 'Use 25 vehicles' is a plan setting, never 25 new vehicles. One vehicle per call: it is a type with its capacity, and how many units a plan uses is count on that plan. action=create takes vehicle (name, max_weight_kg, max_volume_m3); a name the account already has returns that vehicle (outcome=already_existed) when the capacities match and NAME_TAKEN when they differ: ask whether to update it or use another name. action=update takes vehicle_id (list_fleet vehicles) and changes. Say what will be saved and get a yes first. Does not consume plan stops.

- Read only: no · Destructive: yes · Idempotent: yes · Open world: no
- Published: `MCP_CATALOG_WRITE_TOOLS_ENABLED`
- Workflows: Save a vehicle or a depot

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `action` | create \| update | yes | create saves a new vehicle; update changes one the account already has. |
| `vehicle` | object | no | For create: the vehicle to save. |
| `vehicle.name` | string | yes | What the user calls it, e.g. 'Sprinter'. |
| `vehicle.max_weight_kg` | number | no | Payload capacity in kilograms. Omit when the user did not give one: never invent a capacity. |
| `vehicle.max_volume_m3` | number | no | Cargo volume in cubic meters. Omit when the user did not give one. |
| `vehicle_id` | string | no | For update: a vehicle_id from list_fleet's top-level vehicles. |
| `changes` | object | no | For update: the fields to change. |
| `changes.name` | string | no | New name. |
| `changes.max_weight_kg` | number | no | New payload capacity, kg. |
| `changes.max_volume_m3` | number | no | New cargo volume, m3. |

### Output

| Field | Type | Description |
|---|---|---|
| `vehicle` | any |  |
| `outcome` | created \| updated \| already_existed | created, updated, or already_existed: the account had this exact one, so nothing was written. Say which to the user. |
| `account_url` | string | Opens the account's saved vehicles in the Vepathos dashboard. Requires signing in. |

## `manage_depot`

**Manage saved depots**

Add or change a depot saved in the user's Vepathos account, the place routes start from: master data that stays after this conversation. Call it only when the user asks to add, save, rename or move a depot. A depot for one run is a plan setting: pass its coordinates as depot to optimize_delivery_routes or optimize_plan and save nothing. To use a saved one ('use the Barracas depot'), read list_fleet depots and pass its coordinates. Takes latitude and longitude: an address goes through geocode_addresses first; tell the user the matched address before saving and do not invent coordinates. action=create takes depot (name, latitude, longitude); a name the account already has returns that depot (outcome=already_existed) when it is the same place and NAME_TAKEN otherwise. action=update takes depot_id (list_fleet depots) and changes. Say what will be saved and get a yes first. Does not consume plan stops.

- Read only: no · Destructive: yes · Idempotent: yes · Open world: no
- Published: `MCP_CATALOG_WRITE_TOOLS_ENABLED`
- Workflows: Save a vehicle or a depot

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `action` | create \| update | yes | create saves a new depot; update renames or moves one the account already has. |
| `depot` | object | no | For create: the depot to save. |
| `depot.name` | string | yes | What the user calls it, e.g. 'Barracas'. |
| `depot.latitude` | number | yes | Decimal degrees (WGS84). From geocode_addresses when given an address. |
| `depot.longitude` | number | yes | Decimal degrees (WGS84). |
| `depot_id` | string | no | For update: a depot_id from list_fleet's depots. |
| `changes` | object | no | For update: the fields to change. |
| `changes.name` | string | no | New name. |
| `changes.latitude` | number | no | New latitude; send longitude too. |
| `changes.longitude` | number | no | New longitude; send latitude too. |

### Output

| Field | Type | Description |
|---|---|---|
| `depot` | any |  |
| `outcome` | created \| updated \| already_existed | created, updated, or already_existed: the account had this exact one, so nothing was written. Say which to the user. |
| `account_url` | string | Opens the account's saved depots in the Vepathos dashboard. Requires signing in. |

## `list_automations`

**List automations**

List the connected Vepathos account's standing rules: when each one looks, how many waiting orders are worth a run, which orders qualify, its vehicles, whether it asks first (suggest) or routes by itself (auto), whether it is switched on, and what it last decided. It also returns the stores the account has connected, with the integration_account_id create_automation takes. Takes no arguments; read-only and it does not consume plan stops. empty=true: no automation yet. plan_missing=true: its plan was deleted and it cannot run until the user fixes that in the dashboard.

- Read only: yes · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Prepare an automation

### Input

Takes no arguments.

### Output

| Field | Type | Description |
|---|---|---|
| `automations` | array of any |  |
| `stores` | array of any | Stores the account has connected. |
| `limits` | any |  |
| `empty` | boolean | True: no automation yet. |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |

## `create_automation`

**Prepare an automation**

Prepare a rule that routes deliveries on a schedule. template_plan_id (from list_plans) is COPIED for its depot, vehicles and settings, so editing that plan later does not change the rule. Say where the batch comes from: use_plan_stops for a route that repeats, or integration_account_id for a connected store. Say when it looks: looks_at for one time a day, or window_from/window_to with every_minutes. Send your own operation_id; the same one twice returns the same rule, never a second. It is always created SWITCHED OFF and decides nothing: only the user turns one on, in the dashboard, because a rule that is on spends their stops unattended. Say what it would do, give them account_url, and never say it is running. `missing` lists what a run would still lack. It costs no plan stops.

- Read only: no · Destructive: no · Idempotent: yes · Open world: no
- Published: always
- Workflows: Prepare an automation

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | string | yes | What to call the rule, e.g. 'Reparto de la mañana'. |
| `template_plan_id` | string | yes | A saved plan (list_plans) to copy the depot, the vehicles and the optimizer settings from. It is copied once, not linked: editing or deleting that plan later does not touch the rule. |
| `use_plan_stops` | boolean | no | True for a route that repeats: the plan's own stops are the batch. False when a store feeds it. |
| `integration_account_id` | string | no | A connected store to take orders from (from list_automations `stores`). |
| `days` | array of integer | no | Days it looks, 0 = Sunday … 6 = Saturday. |
| `looks_at` | string | no | One time of day, HH:MM: it looks once, then. Use this for 'every day at 8'. |
| `window_from` | string | no | Start of a window it looks inside. |
| `window_to` | string | no | End of that window. |
| `every_minutes` | integer | no | How often it looks inside the window. |
| `min_orders` | integer | no | Fewer waiting orders than this and the slot is skipped. |
| `match_tags` | array of string | no | Only orders carrying these tags, e.g. ml:flex, ship:envio-a-domicilio. |
| `vehicle_type_id` | string | no | Template vehicle entry. Omit for a single type; otherwise choose in the dashboard. |
| `look` | clock \| fill \| both | no |  |
| `fill_by` | orders \| packages \| stops \| weight \| volume | no |  |
| `min_packages` | integer | no |  |
| `fill_percent` | integer | no |  |
| `max_units` | integer | no | Vehicles the user can actually put on the street. |
| `stops_per_vehicle` | integer | no | Stops each vehicle takes. |
| `mode` | string | no | suggest: it works the routes out and asks. auto: it routes on its own once switched on. |
| `timezone` | string | no | IANA time zone the schedule is read in. |
| `operation_id` | string | yes | Your id for this attempt. Sending it twice returns the same rule instead of a second one. |

### Output

| Field | Type | Description |
|---|---|---|
| `automation` | any |  |
| `enabled` | boolean | New rules are off. A replay preserves the current state, including a user activation. |
| `replayed` | boolean |  |
| `vehicle_options` | array of any | Template vehicle choices. Select in the dashboard; replay never edits the rule. |
| `missing` | array of string | What a run would still lack (depot, fleet). Empty: the user only has to switch it on. |
| `account_url` | string | Opens the plan in the user's Vepathos account. Requires signing in; private to the account. |

## `create_optimization_map`

**Create route map**

Create a temporary public link to the map of a completed optimization owned by the connected account. A result reaches the user two ways, and the user chooses: the plan in their Vepathos account (account_url, sign-in required) or this link. Offer both; create the link only when the user picks it. Anyone with the link can view the delivery locations without signing in: tell the user this and that it expires 48 hours after creation. Repeating the call returns the same link without extending it; after expiry the page says so. Does not optimize again or charge stops. Pass language with the conversation's language so the page, share message and PDF open in it (en, es, pt); the same link works in any language.

- Read only: no · Destructive: no · Idempotent: yes · Open world: no
- Published: `MCP_MAP_SHARES_ENABLED`
- Workflows: Share a result

### Input

| Field | Type | Required | Description |
|---|---|---|---|
| `optimization_id` | string | yes |  |
| `language` | string | no | Language of the conversation, e.g. es, es-AR, pt-BR, en. The map page opens in it when supported (en, es, pt); otherwise it follows the viewer's browser. |

### Output

| Field | Type | Description |
|---|---|---|
| `map_url` | string |  |
| `expires_at` | string |  |
| `access` | string |  |
| `notice` | string |  |
| `optimization_id` | string |  |
| `language` | en \| es \| pt |  |
| `error` | object |  |
